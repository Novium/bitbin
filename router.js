Router.configure({
  layoutTemplate: 'layout'
});

Router.route('home', {
  path: '/'
});

Router.route('login', {
  path: '/login'
});

Router.route('show', {
  path: '/show'
});

Router.route('new', {
  path: '/new'
});
